import 'package:flutter/material.dart';
import 'package:food_introduce/ui/pages/filter/filter_content.dart';

class FilterMealPage extends StatelessWidget {
  const FilterMealPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(

      ),
      body: FilterMealContent(),

    );
  }
}
